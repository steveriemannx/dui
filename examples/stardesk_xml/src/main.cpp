#include "MainThread.h"

#include "dui/Utils/AppEntry.h"

/** StarDesk XML-mode entry point: the UI is described in the XML skins under
 *  stardesk_xml/, the feature code is shared with the pure-code example. */
DUI_APP_ENTRY(sdk::MainThread)
